[project]
name = "DSAI_Assignments"
version = "0.1.0"
authors = [{ name = "author" }]
description = "A sample project."
requires-python = ">=3.8"

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
